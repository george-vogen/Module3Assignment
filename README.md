For this assignment I really had to work with Copilot to make sure I had the retainAll() and the Hashsets in the right place and working properly, after that putting in the information was pretty easy. While revising the program I added some error handling for null labels and unknown labels which is something I encountered when I was changing the information in the Venn diagram. So instead of the code erroring, it would just throw an error message. Other than that I changed the information in the Venn diagram to the three primary colors instead of prime, even, or Fibonacci numbers.
